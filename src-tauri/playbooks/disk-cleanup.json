{
  "id": "disk-cleanup",
  "name": "Disk Cleanup",
  "description": "Scans temp directories and lists large files for cleanup",
  "category": "productivity",
  "version": "1.0.0",
  "author": "AgentOS",
  "tags": ["disk", "cleanup", "storage"],
  "steps": [
    {
      "id": "step-1",
      "description": "List large files in temp directory",
      "action_type": "shell",
      "command": "Get-ChildItem $env:TEMP -Recurse -ErrorAction SilentlyContinue | Sort-Object Length -Descending | Select-Object -First 20 | Format-Table Name, Length, LastWriteTime"
    }
  ],
  "created_at": "2025-01-01T00:00:00Z"
}
